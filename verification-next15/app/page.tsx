// Server Component 既定（'use client' なし）。版に依らない中核原則。
export default function Page() {
  return (
    <main>
      <h1>nextjs-builder verification app (Next 15)</h1>
      <p>版差（middleware.ts / 非同期リクエストAPI / no &apos;use cache&apos;）の裏取り用。</p>
    </main>
  )
}
