{
  "name": "nextjs-builder-verification-next15",
  "private": true,
  "description": "Maintainer-only harness for the Next 15 path: locks the version-divergent surface (middleware.ts, async request APIs, no 'use cache') so the agent's Next-15-adapted output is proven to build. Complements verification/ (Next 16).",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "build": "next build"
  },
  "dependencies": {
    "next": "15.5.20",
    "react": "19.0.0",
    "react-dom": "19.0.0",
    "server-only": "^0.0.1",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "typescript": "^5"
  }
}
